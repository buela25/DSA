# DSA
Implementation of Linked list, stacks, queues, trees

Linked List - Data can be added at the head/tail 
              However, adding data at the front of list is efficient
              
Queue       - Data can be added only at the end, 
              however , note the tail node at every execution. So directly new node can be appended with tail node. (Efficient)
